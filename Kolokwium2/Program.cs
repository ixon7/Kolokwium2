using System;
using System.Collections;
using System.Collections.Generic;

namespace Kolokwium2
{
    public class Komputer
    {
        public string Procesor, RAM, HDD, Grafika;
        public Komputer(string Procesor, string RAM, string HDD, string Grafika)
        {
            return;
        }
    }
    class Program
    {
        static void Main(string[] args)
        {
            Console.WriteLine("Kolokwium 2:");
            for (int i = 0; i < 100; i++)
            {

            }
            var rand = new Random();
            //Komputer pc = new Komputer();

            //Komputer pcexample = new Komputer();
            IEnumerable<string> enumerable = new string[] {
                
            };
            foreach (string s in enumerable)
                Console.WriteLine(s);
        }
    }
}
